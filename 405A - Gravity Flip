#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    vector<int>v;
    while(n--){
        int m;
        cin>>m;
        v.push_back(m);
    }
    
    sort(v.begin(),v.end());
    for(auto u:v){
        cout<<u<<" ";
    }
}
