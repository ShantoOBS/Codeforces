#include<bits/stdc++.h>
using namespace std;
int main()
{
 string s;

 cin>>s;
  if(s.length()==1){
  if(s[0]=='!' || s[0]=='~' || s[0]=='+'){
      cout<<"NO";
      return 0;
  }
  }
  
   if(s[0]=='Q' || s[0]=='H' || s[0]=='9'){
      cout<<"YES";
      return 0;
  }
  int set=0;
  for(int i=0;i<s.length();i++){
      if(s[i]=='H' || s[i]=='Q' || s[i]=='9'  ){
          set++;
          break;
      }
  }

  if(set ==1){
      cout<<"YES";
  }
  else{
      cout<<"NO";
  }
  
  
  
  
 

}
